<div id="side-nav">
<% unless @subpages.empty? %>
	
		<ul>
			<% for page in @subpages %>
				<li><%= link_to page.navlabel, view_page_path(page.name)%></li>
			<% end %>
		</ul>
<% end %>
</div>

<div id="main">
	<h1><%= @page.title %> </h1>
	
	<%= @page.body %>
</div>